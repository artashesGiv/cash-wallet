import React from 'react'
import {category} from '../../store/Categories/categoriesReducer'

type CategoryItemPropsType = {
   category: category
}

export const CategoryItem = ({}: CategoryItemPropsType) => {
   return (
      <div>

      </div>
   )
}
